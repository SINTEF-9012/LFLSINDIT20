FAST_API_HOST='0.0.0.0'
FAST_API_PORT='9017'

GRAPHDB_HOST='graphdb'
GRAPHDB_PORT='7200'
GRAPHDB_USERNAME="sindit20"
GRAPHDB_PASSWORD="sindit20"
GRPAPHDB_REPOSITORY="SINDIT"

LOG_LEVEL='DEBUG'

USE_HASHICORP_VAULT='False'
FSVAULT_PATH='/app/data/vault.properties'
HASHICORP_URL='http://127.0.0.1:8200'
HASHICORP_TOKEN=''



DOCKER_ENV=True # This is a flag to indicate that the application is running in a docker environment

# LLM / Ollama — host.docker.internal pointe vers la machine hôte depuis Docker Desktop (Windows/Mac)
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=qwen3:8b-16k

# Keycloak configuration
USE_KEYCLOAK=False
KEYCLOAK_SERVER_URL="http://localhost:8080"
KEYCLOAK_REALM="sindit"
KEYCLOAK_CLIENT_ID="sindit"
KEYCLOAK_CLIENT_SECRET="AeHcDu55GTh3PkqaUwKqaRPUPWKqCH7C"

# InMemory authentication configuration
USER_PATH='/app/data/user.json'
WORKSPACE_PATH='/app/data/workspace.json'
